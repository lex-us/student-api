wire-mock.server.port=8081
wire-mock.server.address=localhost
wire-mock.server.url=http://${wire-mock.server.address}:${wire-mock.server.port}

spring.flyway.enabled=true

#Services base url
rest.service.SEND_TO_EMAIL_API.url=${wire-mock.server.url}/api/email/send

logging.level.root=warn
